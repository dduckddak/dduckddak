spring:
  profiles:
    active: ${profile}
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
    hibernate:
        ddl-auto: create # or create, create-drop, validate, none

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type: TRACE